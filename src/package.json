{
  "name": "file-viewer",
  "version": "1.0.0",
  "description": "Simple Node.js server to display a mounted file",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --legacy-watch --watch server.js --watch views --watch scripts --ext js,ejs --exec \"node server.js\"",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "5.19.1",
    "dotenv": "^17.2.1",
    "ejs": "3.1.10",
    "express": "4.19.2",
    "openai": "^4.56.0",
    "onnxruntime-node": "1.20.1",
    "wav-decoder": "^1.3.0"
  },
  "devDependencies": {
    "prisma": "5.19.1",
    "nodemon": "3.1.7"
  }
}
