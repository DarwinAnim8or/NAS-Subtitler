# Exclude large or unnecessary files from Docker build context

# Dependencies
node_modules
.npm

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Git
.git
.gitignore

# Docker files (not needed inside image)
Dockerfile
Dockerfile.*
docker-compose.yml
docker-compose.*.yml

# Env files (provided at runtime via compose)
.env
.env.*

# Prisma local DB (schema and migrations are needed)
prisma/dev.db

# Editors/IDE
.vscode
.idea
*.swp

# OS junk
.DS_Store
Thumbs.db

# Build artifacts and local images
build
dist
coverage
out
*.tar
nasclient-*.tar